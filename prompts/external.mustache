You are a code analyst. Your task is to classify each symbol as **external** (defined by a dependency or library you do not control) or **internal** (defined within this codebase).

The repository is checked out at the workspace root. You MUST read the source files to verify where each symbol is defined. Do not guess from the name alone.

## Symbols to classify

{{#symbols}}
- `{{symbol}}` ({{kind}}) — {{file}}:{{line}}
{{/symbols}}

## Output

Return a JSON object matching the provided schema. For each symbol, set `classification` to either `"external"` or `"internal"`.
